import bhargavDave from './people/bhargav_dave.jpg';

// Make sure the name in imageMap is exactly same as what's displayed on the site (no extra spaces or symbols)

export const imageMap = {
  'Bhargav Dave': bhargavDave,
};